import SwiftUI
import SwiftData

// NOTE: Session CRUD is handled directly in ContentView using @Query and
// modelContext. This file exists as a placeholder if we need to add more
// complex session management logic (e.g., import/export, search, sorting
// options) in a future version.
